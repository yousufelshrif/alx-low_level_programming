#include "main.h"

/**
 * _abs - "Write a function that computes the absolute value of an integer."
 * @n: the number to be cheaked
 * Return: Always abs of the number
 */
int _abs(int n)
{
	if (n < 0)
	{
		n = n * -1;
	}
		return (n);
}
