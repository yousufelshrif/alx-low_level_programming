#include <stdio.h>

/**
 * main - "program that prints the alphabet in lowercase, followed by a new line"
 * Return: Always 0
 */
int main(void)
{
	for (char i=a;i<=z;i++)
	{
		putchar(i);
		putchar("\n");
	}

	return (0);
}
